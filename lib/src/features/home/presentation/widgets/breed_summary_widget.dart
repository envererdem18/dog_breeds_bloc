import 'package:flutter/widgets.dart';

class BreedSummaryWidget extends StatelessWidget {
  const BreedSummaryWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
